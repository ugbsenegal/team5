<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bmd.tsg_mbour">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Acceuil" />
        <activity android:name=".Bureau" />
        <activity android:name=".Volet_sport" />
        <activity android:name=".Volet_education" />
        <activity android:name=".Volet_communication" />
        <activity android:name=".But_du_mvt" />
        <activity android:name=".Inscription"></activity>
    </application>

</manifest>